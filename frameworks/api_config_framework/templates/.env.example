# API 金鑰設定範例
# 複製此檔案為 .env 並填入你的實際金鑰

# ===== Google Gemini API =====

# 方法一：使用單一金鑰（簡單模式）
GOOGLE_API_KEY=your_google_gemini_api_key_here

# 方法二：使用多個金鑰（自動輪替模式 - 推薦）
# 當第一個金鑰遇到配額限制時，系統會自動切換到下一個
# 多個金鑰用逗號分隔，不要有空格
API_KEYS=key1,key2,key3,key4,key5

# ===== OpenAI API（如果使用） =====
# OPENAI_API_KEY=your_openai_api_key_here
# API_KEYS=openai_key1,openai_key2,openai_key3

# ===== Anthropic Claude API（如果使用） =====
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# API_KEYS=anthropic_key1,anthropic_key2,anthropic_key3

# ===== 注意事項 =====
# 1. 如果同時設定了 API_KEYS 和單一金鑰，系統會優先使用 API_KEYS
# 2. 建議使用多金鑰模式以提高工作流程的穩定性
# 3. 金鑰會按順序使用，遇到配額限制時自動切換
# 4. 確認此檔案（.env）已加入 .gitignore，不要提交到版本控制系統！
